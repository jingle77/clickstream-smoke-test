import pandas as pd


def generate_clickstream_data(row_count: int) -> pd.DataFrame:
    """
    Placeholder for synthetic clickstream data generation.
    Real implementation will be added later.
    """
    return pd.DataFrame()